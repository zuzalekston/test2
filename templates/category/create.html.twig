{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title_category_create'|trans }}
{% endblock %}

{% block body %}
    <h1>{{ 'title_category_create'|trans }}</h1>
    {{ form_start(form, { method: 'POST', action: url('category_create') }) }}
    {{ form_widget(form) }}
    <div class="form-group row float-sm-right">
        <input type="submit" value="{{ 'action_add'|trans }}" class="btn btn-primary" />
    </div>
    <div class="form-group row float-sm-left">
        <a href="{{ url('category_index') }}" class="btn btn-link">
            {{ 'action_back_to_categories_list'|trans }}
        </a>
        <a href="{{ url('photo_index') }}" class="btn btn-link">
            {{ 'action_back'|trans }}
        </a>

    </div>
    {{ form_end(form) }}
{% endblock %}