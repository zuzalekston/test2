{% include 'default/_form.html.twig' with {
    page_title: 'title_photo_edit',
    page_title_param: {'%id%': photo.id},
    attr: {
        method: 'PUT',
        action: url('photo_edit', {'id': photo.id}),
    },
    form: form,
    back_to_list_path: 'photo_index',
} %}